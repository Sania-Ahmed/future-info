import React from 'react';

const Blog = () => {
    return (
        <div>
          <h2>When should one use context API ?</h2>
          <p>Developer should use context API when a common data is needed to be accessed by multiple components.</p>
        </div>
    );
};

export default Blog;